import { type } from "os"


export type user = {
   id: number,
   name: string,
   email: string,
   description: string,
}

export type psychologist = {
  id: number;
  name: string;
  email: string;
  description: string;
};